{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Forestar Editor 桌面应用默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ],
  "remote": {
    "urls": ["http://127.0.0.1:5000/*"]
  }
}
